<%= form_for @answerable, remote: true do |f| %>
<ul>
  <li>
    <%= f.hidden_field :question, @question.id %>
    <%= f.hidden_field :user, @user.id %>
    <%= f.text_area :body %>
  </li>
  <li>
    <%= f.submit "Submit Answer" %>
  </li>
</ul>
<% end %>
<div id="markdown" class='body'></div>
